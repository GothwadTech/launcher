# Build
*.iml
.gradle/
.kotlin/
build/
app/build/
/local.properties
/captures
.externalNativeBuild
.cxx

# IDE
.idea/
*.hprof
.claude/

# OS
.DS_Store

# Keystores / secrets
# debug.keystore is deliberately NOT un-ignored: CI generates its own copy under
# ~/.android/ and local builds fall back to the standard debug key, so nothing here
# should ever be committed (issue #44).
*.jks
*.keystore

# Misc
/tmp/

# Session transcripts / exports
pi-session-*.html
*.session.html
.env
.dsx/
